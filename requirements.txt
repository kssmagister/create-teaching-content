# Schema-Validierung (Build laeuft auch ohne, dann ungeprueft)
jsonschema>=4.0

# Web-Schicht (FastAPI): Verwaltung von Units/Dateien, Build ausloesen, PDF liefern
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# LLM-Automatisierung (Schritt 3: agent.py)
anthropic>=0.75
pypdf>=5.0

# Rendering uebernimmt das Typst-CLI (im Container installiert, lokal via winget/scoop).
# Optionale Multiformat-Konvertierung via Pandoc (separat).
